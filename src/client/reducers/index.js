const initialState = {
  singleUser: {},
  puppies: []
};

// ACTION TYPES

// ACTION CREATORS

// THUNK CREATORS

// REDUCER
const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default rootReducer;
